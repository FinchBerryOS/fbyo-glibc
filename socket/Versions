libc {
  GLIBC_2.0 {
    # functions used in other libraries
    __connect; __send;

    # a*
    accept;

    # b*
    bind;

    # c*
    connect;

    # g*
    getpeername; getsockname; getsockopt;

    # i*
    isfdtype;

    # l*
    listen;

    # r*
    recv; recvfrom; recvmsg;

    # s*
    send; sendmsg; sendto; setsockopt; shutdown; socket; socketpair;
  }
  GLIBC_2.2.4 {
    # Addition from P1003.1-200x
    sockatmark;
  }
  GLIBC_2.10 {
    accept4;
  }
  GLIBC_2.17 {
    recvmmsg; sendmmsg;
  }
  FINCHLIBC_1.0 {
    fbsocket_open;
    fbsocket_upgrade;
    fbsocket_connect;
    fbsocket_close;
    fbsocket_write;
    fbsocket_set_reader;
    fbsocket_set_reader_ack;
    fbsocket_send_ack;
    fbsocket_get_state;
  }
  GLIBC_PRIVATE {
    __sendmmsg;
    __recv; __socket;
  }
}
